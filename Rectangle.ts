class Rectangle {
    topLeft: Vector;

    width: number;
    height: number;

    constructor(topLeft: Vector, width: number, height: number) {
        this.topLeft = topLeft;
        
        this.width = width;
        this.height = height;
    }
}
